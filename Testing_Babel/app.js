// Testing Babel
const age = 100;
const people = ['Wilber', 'Josue'];
const fullNames = people.map(name => `${name} Castaneda`);

// If I need to add plugins I can do it after the "targets" ending square brackets
